我需要你为这个 mi-gui 实现以下三个页面, 一个简单的效果图位于 THEME.png.

1. 一个显示识别卫星的情况的页面, 这个页面是一个非模态对话框, 主体可以是一个嵌入浏览器 SDK 的控件, 内部要对接 Google Map SDK. SDK 中要显示指定地区的卫星地图, 显示用户终端的位置, 用户终端最好能用手机图标来表示. 另外要显示卫星的位置 (具体坐标由我们提供, 传入参数) 和卫星的名字 (也由脚本传入). 以卫星为中心, 显示一个指定半径的圆, 用蓝色半透明填充, 标示卫星的覆盖范围. 在地图的底下, 要有一个表格, 展示卫星距离用户的距离, 卫星的仰角和方位角. 这个表格的数据也由脚本传入. 另外, 要有一个按钮, 点击后可以重新加载卫星数据 (重新调用脚本获取最新的卫星数据). 在当前阶段, 脚本可以直接返回任意的模拟数据.

2. 一个显示卫星轨道和服务情况的界面, 在这个页面里, 大体布局与上一个相同, 通过星历 (动态通过脚本传入) 来显示卫星的当前位置 (通过脚本传入坐标), 以及卫星的轨道 (通过脚本传入轨道数据). 这个页面的底下需要一个表格, 显示卫星的剩余服务时间和进入下一个卫星的时间 (数据通过脚本传入). 另外, 也需要一个按钮, 点击后可以重新加载卫星轨道和服务数据 (重新调用脚本获取最新的数据). 在当前阶段, 脚本可以直接返回任意的模拟数据.

3. 第三个页面里面我需要你实时的显示两幅 matplotlib 的图 (这个图会实时更新, 你需要预留更新的接口), 第一幅图是时间 (当前时间) 与多普勒频移之间的关系, 横轴是时间, 每次更新的时候, 我们会提供最新的数据; 纵轴是频移, 以 kHz 为单位. 有两根线, 一根是散点代表实际测量的结果; 一个是平滑曲线代表根据 TLE 信息做出的预测. 第二幅图是距离与时间的关系, 要求与上一幅图一致. 两幅图上下 1:1 占满这个非模态对话框. 在当前阶段, 可以用一个模拟脚本生成正弦曲线来模拟.

以上是实现的分别的要求, 还有以下整体的要求:

- 所有的都用英文, 英文注释, 英文图例.
- 在行内给出详细的文档, 用英文写
